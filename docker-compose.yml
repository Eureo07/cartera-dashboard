services:
  dashboard:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    # Optional: set your Gmail app password for email alerts
    # - GMAIL_PASSWORD=your_app_password_here
